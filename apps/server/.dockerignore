# Server-specific dockerignore for building inside Docker
# This is used when building from the ROOT context with -f apps/server/Dockerfile

# We NEED node_modules and dist from the build context
# So DON'T exclude them here

# Exclude git
.git
.gitignore
.gitattributes

# Exclude CI/CD
.github

# Exclude docs
*.md
docs/

# Exclude logs
*.log
logs/

# Exclude editor
.vscode
.idea
*.swp

# Exclude OS files
.DS_Store
Thumbs.db

# Exclude tests (saves space)
**/*.test.*
**/*.spec.*
__tests__/
coverage/

# Exclude temporary
tmp/
temp/
.turbo

# Exclude env files (pass via --env-file or env vars)
.env
.env.*

# Build outputs
dist
build
.next
.turbo