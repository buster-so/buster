{
  "extends": ["//"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "build:standalone": {
      "dependsOn": ["build"],
      "outputs": ["dist/buster"],
      "cache": false
    }
  }
}
