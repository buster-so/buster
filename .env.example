# =============================================================================
# Environment Configuration
# =============================================================================
# This file contains example configuration for environment variables.
# Copy this file to .env and fill in the values.
#
# Runtime secrets are managed via Infisical through the @buster/secrets package.
# Only build-time variables (NEXT_PUBLIC_*) need to be set as environment variables.
# =============================================================================

# -----------------------------------------------------------------------------
# Infisical Configuration (REQUIRED)
# -----------------------------------------------------------------------------
# These credentials are required to fetch runtime secrets from Infisical
INFISICAL_CLIENT_ID=
INFISICAL_CLIENT_SECRET=
INFISICAL_PROJECT_ID=
INFISICAL_ENVIRONMENT=development
INFISICAL_SITE_URL=https://app.infisical.com

# -----------------------------------------------------------------------------
# Build-Time Variables (REQUIRED)
# -----------------------------------------------------------------------------
# These MUST be set as environment variables because they're needed at build time
# for the Next.js app. They cannot be fetched from Infisical during build.

# Next.js Public Variables (exposed to browser - required at build time)
NEXT_PUBLIC_API_URL=http://localhost:8000
NEXT_PUBLIC_API2_URL=http://localhost:8001
NEXT_PUBLIC_WEB_SOCKET_URL=ws://localhost:8000
NEXT_PUBLIC_URL=http://localhost:3000
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
NEXT_PUBLIC_POSTHOG_KEY=
NEXT_PUBLIC_POSTHOG_HOST=https://app.posthog.com

# Build Control Variables
NODE_ENV=development
ANALYZE=false
NEXT_DISABLE_LINT=false
NEXT_DISABLE_TS_CHECK=false
CI=false

# -----------------------------------------------------------------------------
# Notes
# -----------------------------------------------------------------------------
# 1. Infisical credentials are REQUIRED for the application to run
# 2. Build-time variables (NEXT_PUBLIC_*) MUST be set as environment variables
# 3. All other runtime secrets are managed in Infisical
# 4. For local development, you can override Infisical secrets by setting them as env vars
# 5. The @buster/secrets package checks env vars first, then fetches from Infisical