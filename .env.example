# =============================================================================
# Environment Configuration
# =============================================================================
# This file contains example configuration for environment variables.
# Copy this file to .env and fill in the values.
#
# Runtime secrets are managed via Infisical through the @buster/secrets package.
# Only build-time variables (NEXT_PUBLIC_*) need to be set as environment variables.
# =============================================================================

# -----------------------------------------------------------------------------
# Infisical Configuration (REQUIRED for centralized secrets)
# -----------------------------------------------------------------------------
# Shared development team credentials - get these from your team lead
# Local .env values will override Infisical secrets (for local development overrides)
INFISICAL_CLIENT_ID=  # Shared development machine identity Client ID
INFISICAL_CLIENT_SECRET=  # Shared development machine identity Client Secret  
INFISICAL_PROJECT_ID=  # Your Infisical project ID
INFISICAL_ENVIRONMENT=development  # Use 'development' for local dev
INFISICAL_SITE_URL=https://app.infisical.com  # Or your self-hosted Infisical URL

# -----------------------------------------------------------------------------
# Build-Time Variables (REQUIRED)
# -----------------------------------------------------------------------------
# These MUST be set as environment variables because they're needed at build time
# for the Next.js app. They cannot be fetched from Infisical during build.

# Next.js Public Variables (exposed to browser - required at build time)
NEXT_PUBLIC_API_URL=http://localhost:8000
NEXT_PUBLIC_API2_URL=http://localhost:8001
NEXT_PUBLIC_WEB_SOCKET_URL=ws://localhost:8000
NEXT_PUBLIC_URL=http://localhost:3000
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
NEXT_PUBLIC_POSTHOG_KEY=
NEXT_PUBLIC_POSTHOG_HOST=https://app.posthog.com

# Build Control Variables
NODE_ENV=development
ANALYZE=false
NEXT_DISABLE_LINT=false
NEXT_DISABLE_TS_CHECK=false
CI=false

# -----------------------------------------------------------------------------
# Runtime Secrets (OPTIONAL - managed by Infisical)
# -----------------------------------------------------------------------------
# These secrets are centrally managed in Infisical. You can optionally override
# them locally by uncommenting and setting values below.
# 
# The @buster/secrets package follows this hierarchy:
#   1. First checks local .env file (if value is set here)
#   2. Then fetches from Infisical (using credentials above)
#   3. Throws error if not found in either location
#
# Examples of secrets you can override locally (uncomment as needed):
# DATABASE_URL=
# SUPABASE_SERVICE_ROLE_KEY=
# OPENAI_API_KEY=
# ANTHROPIC_API_KEY=
# SLACK_CLIENT_ID=
# SLACK_CLIENT_SECRET=
# GITHUB_APP_PRIVATE_KEY_BASE64=
# TRIGGER_SECRET_KEY=
# 
# For a full list of available secrets, check packages/secrets/src/keys/

# -----------------------------------------------------------------------------
# Notes
# -----------------------------------------------------------------------------
# 1. Infisical credentials connect you to centralized development secrets
# 2. NEXT_PUBLIC_* variables are REQUIRED and must be set locally (build-time)
# 3. All other secrets are OPTIONAL locally (fetched from Infisical if not set)
# 4. Local .env values always override Infisical (useful for testing)
# 5. Never commit actual secret values to the repository